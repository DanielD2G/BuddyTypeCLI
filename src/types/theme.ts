export interface ThemeColors {
  name: string;
  bg: string;
  text: string;
  textDim: string;
  correct: string;
  incorrect: string;
  extra: string;
  cursor: string;
  accent: string;
  stats: string;
}
